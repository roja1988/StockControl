<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.ScaleMapper">

<!-- SQL スケールマスター -->

	<select id="selectAll" resultType="Scale">
		SELECT
		scale_id,
		scale,
		delete_flg
		FROM master_scale;
	</select>

</mapper>