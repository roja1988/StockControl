<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.MakerMapper">

	<!-- SQL メーカーマスター -->
	
	<select id="selectAll" resultType="Maker">
		SELECT
		maker_id,
		maker,
		delete_flg
		FROM master_maker;
	</select>

</mapper>